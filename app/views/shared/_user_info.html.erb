<% @user ||= current_user %>
<div class="user_info">
  <a href="<%= user_path(@user) %>">
    <%= gravatar_for @user, :size => 60 %>
    <span class="user_name">
      <%= @user.name %>
    </span>
    <span class="posts">
      <%= pluralize(@user.posts.count, "post") %>
    </span>
  </a>
</div>